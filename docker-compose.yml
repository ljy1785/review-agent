services:
  agent:
    build: .
    env_file: .env
    restart: always
    expose:
      - "8080"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/agent.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - agent
    restart: always